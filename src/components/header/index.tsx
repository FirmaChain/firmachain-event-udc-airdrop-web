import React from 'react';
import { HeaderContainer, Wrapper } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <Wrapper></Wrapper>
    </HeaderContainer>
  );
};

export default React.memo(Header);
