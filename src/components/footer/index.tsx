import React from 'react';
import { FooterContainer, Wrapper } from './styles';

const Footer = () => {
  return (
    <FooterContainer>
      <Wrapper></Wrapper>
    </FooterContainer>
  );
};

export default React.memo(Footer);
